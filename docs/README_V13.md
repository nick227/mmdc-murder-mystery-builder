v13 Overlay — Cost + Redundancy + Semantic Weight Balance

Goals:
- reduce token waste (scope editors)
- prevent duplicate cards
- enforce semantic narrative balance
- keep full quality

Changes:
- scoped card_quality_agent prompt
- scoped solvability validator
- semantic narrative weighting
- duplicate detection pruning
- lighter final editor context

Drop-in overlay. No pipeline changes required.
