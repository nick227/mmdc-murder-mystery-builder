export function buildCharacterNames(playerCount) {
  const names = ['Clara Bellamy', 'Jonah Vale', 'Lionel Thorne', 'Evelyn March', 'Becky Wren', 'Otis Pike'];
  return names.slice(0, playerCount);
}
